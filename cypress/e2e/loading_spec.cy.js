describe('template spec', () => {

  // it('Should display loading cards and image', () => {
  //   cy.intercept("POST", "https://garden-grow-be.herokuapp.com/api/v1/graphql", (req) => {
  //     req.delay(3000)
  //     }
  //   );
  //   cy.wait(1000)
  //   cy.visit("http://localhost:3000/results/80910");
  //   cy.get('.loading-card').should('have.length', 6)
  // })
})

// add test for what image is going to be called in loading page